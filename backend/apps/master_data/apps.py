from django.apps import AppConfig

class MastersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.master_data'
    verbose_name = 'Quản lý dữ liệu tổng quát'